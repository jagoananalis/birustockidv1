# Producer Studio v13 — Permanent Three-Column Workspace

Repo target: **Producer `birustockidv1` only**.

Changes:
- Desktop workspace uses three columns: Library → Editor → Public Preview.
- Analysis preview is always visible; no toggle/close overlay.
- News and Education keep the same three-column structure.
- Preview panel is sticky and independently scrollable so it cannot cover the editor.
- The three-column layout remains active down to 1180px viewport width; below that it gracefully collapses for smaller screens.
- Added a small “Preview permanen” indicator in the Analysis toolbar.

TanStack route generation is untouched.
